<%= form_with(model: skill, local: true) do |form| %>
  <% if skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(skill.errors.count, "error") %> prohibited this skill from being saved:</h2>

      <ul>
      <% skill.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <!-- necesito hacer que con el selector se puedan elegir los datos -->


  <div class="field">
    <%= form.label :name, "Nombre de la habilidad" %><br/ >
    <%= form.select :name, Skill.pluck(:name) %>
    
  </div>

  <div class="field">
    <%= form.label :description, "Descripción de la habilidad" %><br/ >
    <%= form.text_area :description, id: :skill_description %>
  </div>

  <div class="field">
    <%= form.label :price, "Precio por hora" %><br/ >
    <%= form.number_field :price, id: :skill_price %>
  </div>
  <br>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
